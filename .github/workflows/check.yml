name: Check

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]
  workflow_dispatch:

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - name: Install Nix
        uses: cachix/install-nix-action@v24
        with:
          github_access_token: ${{ secrets.GITHUB_TOKEN }}

      - name: Setup cache
        uses: actions/cache@v4
        with:
          path: |
            /nix/store
            ~/.cache/nix
          key: ${{ runner.os }}-nix-${{ hashFiles('flake.lock') }}
          restore-keys: |
            ${{ runner.os }}-nix-

      - name: Check flake
        run: nix flake check --show-trace

      - name: Verify package builds
        run: nix build .#default --dry-run --show-trace

      - name: Verify homeManagerModules export
        run: |
          nix eval .#homeManagerModules.default --apply 'x: "ok"'

      - name: Verify overlay export
        run: |
          nix eval .#overlays.default --apply 'x: "ok"'

      - name: Summary
        if: always()
        run: |
          echo "## Check Results" >> $GITHUB_STEP_SUMMARY
          echo "Flake check status: ${{ job.status }}" >> $GITHUB_STEP_SUMMARY
